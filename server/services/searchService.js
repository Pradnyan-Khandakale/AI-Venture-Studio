import { search } from "duck-duck-scrape";

export async function searchMarketSignals(query) {
  // TODO: Query Tavily when TAVILY_API_KEY is set, fall back to a DuckDuckGo search, and
  // TODO: return the joined title / description lines (or an empty string on failure).
  return "";
}
