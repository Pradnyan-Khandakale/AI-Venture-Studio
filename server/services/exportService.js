import PDFDocument from "pdfkit";

export function projectMarkdown(project) {
  // TODO: Build the blueprint Markdown: title, executive summary, overall health score,
  // TODO: and every generated agent report separated by rules.
  return "";
}

export function writeProjectPdf(project, stream) {
  // TODO: Stream a PDF with the title page, overall score, executive summary, and one page
  // TODO: per generated agent report.
}
